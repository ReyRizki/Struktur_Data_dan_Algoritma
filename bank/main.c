#include "service.h"

int main()
{
    // u int N, i;

    // scanf("%u", &N);
    // Customer *customer = (Customer *) malloc(N * sizeof(Customer));

    // REP(i, 0, N)
    //     scanCustomer(customer + i);

    // REP(i, 0, N)
    //     printCustomer(*(customer + i));

    // Staff staff = createStaff();
    // assignStaff(&staff, createTime(1, 2, 3));
    // assignStaff(&staff, createTime(3, 2, 3));
    
    // printStaff(staff);

    // Customer customer = createCustomer(createTime(0, 0, 0), 0, 'x');
    // Customer customer;

    // scanCustomer(&customer);
    // printCustomer(customer);

    


    return 0;
}